# go-lambda
This ex tests the go AWS lambda

Running:

        terraform init
  
        terraform fmt
  
        terraform plan
  
        terraform apply
        
        GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o main main.go
